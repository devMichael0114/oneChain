import React from 'react'
import { Settings as SettingsController } from '../components/Settings'

const Settings = (props) => {
  return (
    <>
      <SettingsController {...props} />
    </>
  )
}

export default Settings
